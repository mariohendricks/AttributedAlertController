import XCTest
@testable import AttributedAlertController

final class AttributedAlertControllerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AttributedAlertController().text, "Hello, World!")
    }
}
